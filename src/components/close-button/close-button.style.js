import { makeStyles } from '@material-ui/core'

export default makeStyles({
  root: {
    // height: '100%',
    minWidth: 50,
    borderRadius: 4,

    '&:hover': {
      backgroundColor: 'transparent',
    },
  },
})
